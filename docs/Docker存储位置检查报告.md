# Docker 存储位置检查报告

> **检查时间**: 2026-01-15  
> **检查人**: 系统自动检查

## 📊 检查结果总结

### ✅ 已在 D 盘（无需迁移）

1. **WSL2 Docker 数据**
   - 路径: `D:\Docker\wsl`
   - 大小: **6.69 GB**
   - 说明: 包含 Docker 镜像、容器、卷等所有数据
   - 状态: ✅ 已正确配置

2. **Docker 镜像和容器**
   - 存储位置: WSL2 虚拟磁盘中（`D:\Docker\wsl\DockerDesktopWSL`）
   - 当前使用:
     - 镜像: 7 个，总计 4.794 GB
     - 容器: 0 个
     - 构建缓存: 67 个，总计 2.248 GB
   - 状态: ✅ 已在 D 盘

3. **Docker 卷（Volumes）**
   - 存储位置: WSL2 虚拟磁盘中
   - 状态: ✅ 已在 D 盘

### ⚠️ 在 C 盘（可以迁移）

1. **Docker 配置目录**
   - 路径: `C:\Users\wangx\.docker`
   - 大小: **约 0.16 GB**
   - 内容:
     - `bin/` - Docker 二进制文件
     - `buildx/` - Docker Buildx 插件
     - `cli-plugins/` - CLI 插件
     - `contexts/` - Docker 上下文
     - `desktop-build/` - Desktop 构建文件
     - `models/` - 模型文件
     - `modules/` - 模块文件
     - `config.json` - 配置文件
     - `daemon.json` - 守护进程配置
   - 状态: ⚠️ 可以迁移到 D 盘

### 📌 必须在 C 盘（无法迁移）

1. **Docker Desktop 程序文件**
   - 路径: `C:\Program Files\Docker\Docker`
   - 说明: Docker Desktop 的可执行文件，必须安装在默认位置
   - 状态: ✅ 正常（这是必要的）

## 🔧 迁移建议

### 需要迁移的内容

**Docker 配置目录** (`C:\Users\wangx\.docker`)

可以使用提供的迁移脚本：
```powershell
# 以管理员身份运行 PowerShell
cd D:\Demo\test-genius
.\scripts\migrate-docker-config.ps1
```

或者手动迁移：
1. 停止 Docker Desktop
2. 复制 `C:\Users\wangx\.docker` 到 `D:\Docker\.docker`
3. 删除原目录
4. 创建符号链接：`mklink /D C:\Users\wangx\.docker D:\Docker\.docker`
5. 启动 Docker Desktop

## 📈 空间占用统计

| 位置 | 路径 | 大小 | 状态 |
|------|------|------|------|
| WSL2 数据 | `D:\Docker\wsl` | 6.69 GB | ✅ D盘 |
| Docker 配置 | `C:\Users\wangx\.docker` | 0.16 GB | ⚠️ C盘（可迁移） |
| Docker 程序 | `C:\Program Files\Docker\Docker` | - | ✅ C盘（必需） |

**总计在 C 盘**: 约 0.16 GB（可迁移）  
**总计在 D 盘**: 约 6.69 GB（已迁移）

## ✅ 结论

1. **主要数据已在 D 盘**: Docker 的所有镜像、容器、卷数据（6.69 GB）已经存储在 D 盘 ✅
2. **配置目录可迁移**: 只有 0.16 GB 的配置目录在 C 盘，可以迁移（可选）
3. **程序文件在 C 盘**: Docker Desktop 程序文件必须在 C 盘，这是正常的

**建议**: 
- 如果 C 盘空间充足，可以不迁移配置目录（仅 0.16 GB）
- 如果 C 盘空间紧张，可以使用迁移脚本将配置目录迁移到 D 盘

## 🔍 验证命令

检查 Docker 数据位置：
```powershell
# 检查 WSL2 数据位置
wsl --list --verbose

# 检查 Docker 系统使用情况
docker system df

# 检查 Docker 配置目录大小
Get-ChildItem "$env:USERPROFILE\.docker" -Recurse | Measure-Object -Property Length -Sum
```

