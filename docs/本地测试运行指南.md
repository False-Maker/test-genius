# 本地测试运行指南

> **文档版本**: 1.0  
> **最后更新**: 2024-01-13  
> **适用环境**: Windows / Linux / macOS

本文档详细说明如何在本地运行测试设计助手系统的各种测试，包括单元测试、集成测试、功能测试等。

---

## 📋 目录

1. [环境准备](#一环境准备)
2. [启动测试环境](#二启动测试环境)
3. [运行Java后端测试](#三运行java后端测试)
4. [运行Python服务测试](#四运行python服务测试)
5. [运行前端测试](#五运行前端测试)
6. [运行集成测试](#六运行集成测试)
7. [功能测试验证](#七功能测试验证)
8. [查看测试结果](#八查看测试结果)
9. [常见问题排查](#九常见问题排查)

---

## 一、环境准备

### 1.1 必需软件

在开始测试之前，确保已安装以下软件：

| 软件 | 版本要求 | 下载地址 | 验证命令 |
|------|---------|---------|---------|
| Java | 17+ | [Oracle JDK](https://www.oracle.com/java/technologies/downloads/) 或 [OpenJDK](https://adoptium.net/) | `java -version` |
| Maven | 3.6+ | [Maven官网](https://maven.apache.org/download.cgi) | `mvn -version` |
| Python | 3.10+ | [Python官网](https://www.python.org/downloads/) | `python --version` |
| Node.js | 18+ | [Node.js官网](https://nodejs.org/) | `node -version` |
| npm | 9+ | 随Node.js安装 | `npm -version` |
| Docker | 最新版 | [Docker官网](https://www.docker.com/get-started) | `docker --version` |
| Docker Compose | 最新版 | 随Docker安装 | `docker-compose --version` |
| PostgreSQL | 14+ | [PostgreSQL官网](https://www.postgresql.org/download/) | `psql --version` |
| Redis | 7+ | [Redis官网](https://redis.io/download) | `redis-cli --version` |

### 1.2 验证环境

运行以下命令验证环境是否正确安装：

```bash
# 检查Java
java -version
# 应该显示：openjdk version "17" 或更高版本

# 检查Maven
mvn -version
# 应该显示：Apache Maven 3.6.x 或更高版本

# 检查Python
python --version
# 应该显示：Python 3.10.x 或更高版本

# 检查Node.js
node -version
# 应该显示：v18.x.x 或更高版本

# 检查Docker
docker --version
# 应该显示：Docker version 20.x.x 或更高版本

# 检查Docker Compose
docker-compose --version
# 应该显示：Docker Compose version v2.x.x 或更高版本
```

### 1.3 克隆项目

如果还没有克隆项目，请先克隆：

```bash
git clone <项目地址>
cd test-genius
```

---

## 二、启动测试环境

### 2.1 使用Docker Compose启动（推荐）

这是最简单的方式，会自动启动所有必需的服务。

#### 步骤1：启动所有服务

```bash
# 在项目根目录执行
docker-compose up -d

# 查看服务状态
docker-compose ps
```

**预期输出**:
```
NAME                        STATUS              PORTS
test-design-postgres        Up (healthy)        0.0.0.0:5432->5432/tcp
test-design-redis          Up (healthy)         0.0.0.0:6379->6379/tcp
test-design-backend-java    Up (healthy)        0.0.0.0:8080->8080/tcp
test-design-backend-python  Up (healthy)        0.0.0.0:8000->8000/tcp
test-design-frontend        Up                  0.0.0.0:3000->3000/tcp
test-design-prometheus      Up                  0.0.0.0:9090->9090/tcp
test-design-grafana         Up                  0.0.0.0:3001->3000/tcp
test-design-alertmanager    Up                  0.0.0.0:9093->9093/tcp
test-design-zipkin          Up                  0.0.0.0:9411->9411/tcp
```

#### 步骤2：等待服务启动完成

等待所有服务状态变为 `Up (healthy)`，通常需要1-2分钟。

```bash
# 持续查看服务状态
docker-compose ps

# 查看服务日志（可选）
docker-compose logs -f backend-java
docker-compose logs -f backend-python
```

#### 步骤3：验证服务健康状态

```bash
# 验证Java后端健康状态
curl http://localhost:8080/actuator/health
# 预期输出：{"status":"UP",...}

# 验证Python AI服务健康状态
curl http://localhost:8000/health
# 预期输出：{"status":"healthy"}

# 验证前端（浏览器访问）
# http://localhost:3000

# 验证数据库连接
docker exec -it test-design-postgres psql -U postgres -d test_design_assistant -c "SELECT version();"

# 验证Redis连接
docker exec -it test-design-redis redis-cli ping
# 预期输出：PONG
```

### 2.2 手动启动服务（可选）

如果不想使用Docker，可以手动启动各个服务：

#### 启动PostgreSQL

```bash
# Windows
# 使用PostgreSQL安装目录的bin目录下的pg_ctl
pg_ctl -D "C:\Program Files\PostgreSQL\14\data" start

# Linux/macOS
sudo systemctl start postgresql
# 或
pg_ctl -D /usr/local/var/postgres start
```

#### 启动Redis

```bash
# Windows
# 使用Redis安装目录下的redis-server.exe
redis-server

# Linux/macOS
redis-server
# 或
sudo systemctl start redis
```

#### 初始化数据库

```bash
# 创建数据库
psql -U postgres -c "CREATE DATABASE test_design_assistant;"

# 执行初始化脚本
psql -U postgres -d test_design_assistant -f database/init/01_init_tables.sql
psql -U postgres -d test_design_assistant -f database/init/02_init_data.sql
psql -U postgres -d test_design_assistant -f database/init/03_example_data.sql
```

---

## 三、运行Java后端测试

### 3.1 进入Java后端目录

```bash
cd backend-java/test-design-assistant-core
```

### 3.2 运行所有测试

```bash
# 运行所有测试（包括单元测试和集成测试）
mvn clean test
```

**预期输出**:
```
[INFO] Scanning for projects...
[INFO] 
[INFO] --- maven-surefire-plugin:3.0.0:test (default-test) @ test-design-assistant-core ---
[INFO] 
[INFO] -------------------------------------------------------
[INFO]  T E S T S
[INFO] -------------------------------------------------------
...
[INFO] Tests run: 231, Failures: 0, Errors: 0, Skipped: 0
[INFO] 
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
```

### 3.3 运行特定测试类

```bash
# 运行需求管理服务测试
mvn test -Dtest=RequirementServiceImplTest

# 运行用例管理服务测试
mvn test -Dtest=TestCaseServiceImplTest

# 运行需求管理Controller测试
mvn test -Dtest=RequirementControllerTest
```

### 3.4 运行特定测试方法

```bash
# 运行特定测试方法
mvn test -Dtest=RequirementServiceImplTest#testCreateRequirement_Success
```

### 3.5 生成测试覆盖率报告

```bash
# 运行测试并生成覆盖率报告
mvn clean test jacoco:report
```

**覆盖率报告位置**:
- HTML报告：`target/site/jacoco/index.html`
- 在浏览器中打开查看详细覆盖率信息

### 3.6 查看测试结果

测试结果会在控制台显示，也可以通过以下方式查看：

```bash
# 查看测试报告（文本格式）
cat target/surefire-reports/*.txt

# 查看测试报告（XML格式）
cat target/surefire-reports/*.xml
```

### 3.7 测试统计

当前Java后端测试统计：
- **总测试用例数**: 231个
- **Service层测试**: 118个（单元测试111个 + 集成测试7个）
- **Controller层测试**: 58个
- **Repository层测试**: 24个
- **其他测试**: 31个

---

## 四、运行Python服务测试

### 4.1 进入Python服务目录

```bash
cd backend-python/ai-service
```

### 4.2 安装测试依赖（如果还没安装）

```bash
# 安装所有依赖（包括测试依赖）
pip install -r requirements.txt

# 或使用虚拟环境（推荐）
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate

# 然后安装依赖
pip install -r requirements.txt
```

### 4.3 运行所有测试

```bash
# 运行所有测试
pytest

# 或使用详细输出
pytest -v
```

**预期输出**:
```
============================= test session starts ==============================
platform win32 -- Python 3.10.x, pytest-8.3.0, pytest-asyncio-0.23.7
collected 25 items

tests/test_llm_service.py ................ [ 64%]
tests/test_model_config_service.py ..... [ 84%]
tests/test_prompt_service.py ........... [100%]

============================= 25 passed in 2.34s ==============================
```

### 4.4 运行特定测试文件

```bash
# 运行LLM服务测试
pytest tests/test_llm_service.py

# 运行模型配置服务测试
pytest tests/test_model_config_service.py

# 运行提示词服务测试
pytest tests/test_prompt_service.py
```

### 4.5 运行特定测试方法

```bash
# 运行特定测试方法
pytest tests/test_llm_service.py::test_call_model_success
```

### 4.6 生成测试覆盖率报告

```bash
# 运行测试并生成覆盖率报告
pytest --cov=app --cov-report=html --cov-report=term

# 查看覆盖率报告
# HTML报告位置：htmlcov/index.html
# 在浏览器中打开查看详细覆盖率信息
```

### 4.7 查看测试结果

```bash
# 查看详细测试输出
pytest -v

# 查看测试输出和打印语句
pytest -s

# 查看失败测试的详细信息
pytest -v --tb=short
```

### 4.8 测试统计

当前Python服务测试统计：
- **测试文件数**: 5个
- **测试用例数**: 约25个
- **测试文件**:
  - `test_llm_service.py` - LLM服务测试
  - `test_model_config_service.py` - 模型配置服务测试
  - `test_prompt_service.py` - 提示词服务测试
  - `test_api_llm.py` - LLM API路由测试
  - `test_api_main.py` - 主应用API测试

---

## 五、运行前端测试

### 5.1 进入前端目录

```bash
cd frontend
```

### 5.2 安装依赖（如果还没安装）

```bash
npm install
```

### 5.3 运行所有测试

```bash
# 运行所有测试（watch模式，文件变化自动重新运行）
npm run test

# 运行所有测试（单次运行）
npm run test:run
```

**预期输出**:
```
 RUN  v1  tests/api/common.test.ts
 ✓ tests/api/common.test.ts (3)
   ✓ getTestLayers should return test layers
   ✓ getTestMethods should return test methods
   ✓ getModelConfigs should return model configs

 RUN  v1  tests/store/user.test.ts
 ✓ tests/store/user.test.ts (11)
   ✓ user store should initialize with default values
   ...

 Test Files  3 passed (3)
      Tests  27 passed (27)
   Start at  2024-01-13 10:00:00
   Duration  1.23s
```

### 5.4 运行测试UI（可视化界面）

```bash
# 启动测试UI（推荐，更直观）
npm run test:ui
```

这会在浏览器中打开测试UI界面（通常是 `http://localhost:51204/__vitest__/`），可以：
- 查看所有测试用例
- 查看测试结果
- 查看测试覆盖率
- 重新运行特定测试

### 5.5 生成测试覆盖率报告

```bash
# 运行测试并生成覆盖率报告
npm run test:coverage
```

**覆盖率报告位置**:
- HTML报告：`coverage/index.html`
- 在浏览器中打开查看详细覆盖率信息

### 5.6 运行特定测试文件

```bash
# 运行特定测试文件
npm run test tests/api/common.test.ts

# 运行特定测试文件（使用Vitest CLI）
npx vitest run tests/store/user.test.ts
```

### 5.7 查看测试结果

测试结果会在控制台显示，也可以通过以下方式查看：

```bash
# 查看测试报告
cat coverage/index.html
```

### 5.8 测试统计

当前前端测试统计：
- **测试文件数**: 3个
- **测试用例数**: 27个
- **测试文件**:
  - `tests/api/common.test.ts` - 通用API测试（3个测试用例）
  - `tests/store/user.test.ts` - 用户Store测试（11个测试用例）
  - `tests/store/cache.test.ts` - 缓存Store测试（13个测试用例）

---

## 六、运行集成测试

### 6.1 前后端集成测试

#### 步骤1：确保所有服务已启动

```bash
# 检查服务状态
docker-compose ps

# 确保所有服务都是 Up (healthy) 状态
```

#### 步骤2：验证API接口

```bash
# 测试需求列表接口
curl http://localhost:8080/api/v1/requirements?page=1&size=10

# 测试用例列表接口
curl http://localhost:8080/api/v1/test-cases?page=1&size=10

# 测试提示词模板列表接口
curl http://localhost:8080/api/v1/prompt-templates?page=1&size=10
```

#### 步骤3：使用Postman或Apifox测试

1. 导入API文档（Swagger）
   - 访问：`http://localhost:8080/api/swagger-ui.html`
   - 导出OpenAPI规范
   - 导入到Postman或Apifox

2. 测试各个接口
   - 创建需求
   - 创建用例
   - 生成用例
   - 等等

### 6.2 Java和Python服务集成测试

#### 步骤1：测试用例生成服务

```bash
# 使用curl测试用例生成接口
curl -X POST http://localhost:8080/api/v1/case-generation/generate \
  -H "Content-Type: application/json" \
  -d '{
    "requirementId": 1,
    "layerId": 1,
    "methodId": 1
  }'
```

#### 步骤2：验证Python服务调用

```bash
# 查看Java后端日志
docker-compose logs -f backend-java

# 查看Python服务日志
docker-compose logs -f backend-python

# 验证Python服务是否被调用
# 在日志中应该能看到Java后端调用Python服务的记录
```

### 6.3 数据库和Redis集成测试

#### 步骤1：测试数据库连接

```bash
# 连接数据库
docker exec -it test-design-postgres psql -U postgres -d test_design_assistant

# 执行查询测试
SELECT COUNT(*) FROM test_requirement;
SELECT COUNT(*) FROM test_case;
SELECT COUNT(*) FROM prompt_template;

# 退出数据库
\q
```

#### 步骤2：测试Redis连接

```bash
# 连接Redis
docker exec -it test-design-redis redis-cli

# 测试基本操作
SET test_key "test_value"
GET test_key
DEL test_key

# 退出Redis
exit
```

---

## 七、功能测试验证

### 7.1 需求管理功能测试

#### 测试步骤

1. **打开前端页面**
   - 访问：`http://localhost:3000`
   - 导航到"需求管理"页面

2. **创建需求**
   - 点击"新建需求"按钮
   - 填写需求信息：
     - 需求名称：测试需求001
     - 需求描述：这是一个测试需求
   - 点击"提交"
   - **验证**：需求编码自动生成（格式：REQ-YYYYMMDD-序号）

3. **查询需求**
   - 在需求列表页面查看
   - 使用搜索功能（按名称搜索）
   - 使用筛选功能（按状态筛选）
   - **验证**：搜索结果正确

4. **更新需求**
   - 点击"编辑"按钮
   - 修改需求信息
   - **验证**：编码字段不可编辑
   - 点击"提交"
   - **验证**：更新成功

5. **状态流转**
   - 将需求状态从"草稿"改为"审核中"
   - 将需求状态从"审核中"改为"已通过"
   - **验证**：状态流转正常

6. **删除需求**
   - 尝试删除"草稿"状态的需求 → 应该成功
   - 尝试删除"已通过"状态的需求 → 应该失败，提示不允许删除

### 7.2 用例管理功能测试

#### 测试步骤

1. **创建用例**
   - 导航到"用例管理"页面
   - 点击"新建用例"按钮
   - 填写用例信息：
     - 用例名称：测试用例001
     - 关联需求：选择刚才创建的需求
     - 测试分层：选择"功能案例测试"
     - 测试方法：选择"场景法"
     - 测试步骤：步骤1、步骤2、步骤3
     - 预期结果：预期结果描述
   - 点击"提交"
   - **验证**：用例编码自动生成（格式：CASE-YYYYMMDD-序号）

2. **用例审核**
   - 选择用例，点击"提交审核"
   - **验证**：用例状态变为"待审核"
   - 点击"审核"按钮
   - 选择"通过"或"不通过"
   - 填写审核意见
   - 点击"提交"
   - **验证**：用例状态更新

3. **用例导入导出**
   - 点击"导出"按钮
   - **验证**：Excel文件下载成功
   - 打开Excel文件，修改数据
   - 点击"导入"按钮，选择修改后的Excel文件
   - **验证**：导入成功，数据正确

### 7.3 用例生成功能测试

#### 测试步骤

1. **单个用例生成**
   - 导航到"用例生成"页面
   - 选择需求（使用之前创建的需求）
   - 选择测试分层：功能案例测试
   - 选择测试方法：场景法
   - 选择模型（可选，默认使用优先级最高的模型）
   - 点击"生成用例"按钮
   - **验证**：任务创建成功，显示任务编码
   - **验证**：任务状态轮询正常（每2秒查询一次）
   - 等待生成完成（通常需要10-30秒）
   - **验证**：生成结果展示（成功/失败统计）
   - 点击"查看用例"按钮
   - **验证**：跳转到用例列表，显示生成的用例

2. **批量用例生成**
   - 在用例生成页面，选择多个需求
   - 点击"批量生成"按钮
   - **验证**：批量任务创建成功
   - **验证**：批量任务进度查询正常
   - 等待所有任务完成
   - **验证**：批量结果统计正确

### 7.4 提示词模板管理测试

#### 测试步骤

1. **创建模板**
   - 导航到"提示词模板"页面
   - 点击"新建模板"按钮
   - 填写模板信息：
     - 模板名称：功能测试模板
     - 模板内容：包含变量 {需求描述}、{测试分层} 等
     - 变量定义：`{"需求描述": "string", "测试分层": "string"}`
   - 点击"提交"
   - **验证**：模板编码自动生成（格式：TMP-YYYYMMDD-序号）

2. **模板变量替换**
   - 选择模板，点击"测试生成"按钮
   - 填写变量值
   - 点击"生成"
   - **验证**：变量替换正确

### 7.5 文件上传和文档解析测试

#### 测试步骤

1. **文件上传**
   - 在需求管理页面，点击"上传文档"按钮
   - 选择Word文档（.docx）或PDF文档（.pdf）
   - 点击"上传"
   - **验证**：上传成功，显示文件URL
   - 尝试上传超大文件（> 100MB）
   - **验证**：上传失败，提示文件过大
   - 尝试上传不支持的文件类型（如.exe）
   - **验证**：上传失败，提示文件类型不支持

2. **文档解析**
   - 在需求管理页面，选择有文档的需求
   - 点击"解析文档"按钮
   - **验证**：文档解析成功，显示解析内容

### 7.6 知识库和用例复用测试

#### 测试步骤

1. **知识库管理**
   - 导航到"知识库管理"页面（如果有）
   - 点击"添加文档"按钮
   - 上传文档
   - **验证**：文档添加成功
   - 使用语义检索功能
   - **验证**：检索结果相关性高

2. **用例复用**
   - 在用例管理页面，选择用例
   - 点击"搜索相似用例"按钮
   - **验证**：显示相似用例列表
   - 创建测试套件
   - **验证**：测试套件创建成功

---

## 八、查看测试结果

### 8.1 Java后端测试结果

#### 控制台输出

测试运行完成后，控制台会显示：
```
[INFO] Tests run: 231, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

#### 测试报告位置

- **文本报告**: `target/surefire-reports/*.txt`
- **XML报告**: `target/surefire-reports/*.xml`
- **覆盖率报告**: `target/site/jacoco/index.html`

#### 查看覆盖率报告

```bash
# 在浏览器中打开覆盖率报告
# Windows
start target/site/jacoco/index.html

# Linux
xdg-open target/site/jacoco/index.html

# macOS
open target/site/jacoco/index.html
```

### 8.2 Python服务测试结果

#### 控制台输出

测试运行完成后，控制台会显示：
```
============================= 25 passed in 2.34s ==============================
```

#### 测试报告位置

- **覆盖率报告**: `htmlcov/index.html`
- **终端报告**: 直接在控制台显示

#### 查看覆盖率报告

```bash
# 在浏览器中打开覆盖率报告
# Windows
start htmlcov/index.html

# Linux
xdg-open htmlcov/index.html

# macOS
open htmlcov/index.html
```

### 8.3 前端测试结果

#### 控制台输出

测试运行完成后，控制台会显示：
```
Test Files  3 passed (3)
     Tests  27 passed (27)
```

#### 测试报告位置

- **覆盖率报告**: `coverage/index.html`
- **测试UI**: `http://localhost:51204/__vitest__/`（运行 `npm run test:ui` 时）

#### 查看覆盖率报告

```bash
# 在浏览器中打开覆盖率报告
# Windows
start coverage/index.html

# Linux
xdg-open coverage/index.html

# macOS
open coverage/index.html
```

### 8.4 监控和日志查看

#### 查看服务日志

```bash
# 查看Java后端日志
docker-compose logs -f backend-java

# 查看Python服务日志
docker-compose logs -f backend-python

# 查看所有服务日志
docker-compose logs -f
```

#### 查看监控指标

- **Prometheus**: `http://localhost:9090`
- **Grafana**: `http://localhost:3001`（用户名：admin，密码：admin）
- **Zipkin**: `http://localhost:9411`

---

## 九、常见问题排查

### 9.1 环境问题

#### 问题1：Java版本不匹配

**错误信息**:
```
UnsupportedClassVersionError
```

**解决方法**:
```bash
# 检查Java版本
java -version

# 应该显示Java 17或更高版本
# 如果不是，请安装Java 17+
```

#### 问题2：Maven依赖下载失败

**错误信息**:
```
Could not resolve dependencies
```

**解决方法**:
```bash
# 清理Maven缓存
mvn clean

# 强制更新依赖
mvn clean install -U

# 如果使用国内网络，可以配置Maven镜像
# 编辑 ~/.m2/settings.xml，添加阿里云镜像
```

#### 问题3：Python依赖安装失败

**错误信息**:
```
ERROR: Could not find a version that satisfies the requirement
```

**解决方法**:
```bash
# 升级pip
pip install --upgrade pip

# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或使用虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

#### 问题4：Node.js依赖安装失败

**错误信息**:
```
npm ERR! network timeout
```

**解决方法**:
```bash
# 使用国内镜像源
npm config set registry https://registry.npmmirror.com

# 或使用cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

### 9.2 Docker问题

#### 问题1：Docker服务启动失败

**错误信息**:
```
Error response from daemon
```

**解决方法**:
```bash
# 检查Docker是否运行
docker ps

# 重启Docker服务
# Windows/macOS: 重启Docker Desktop
# Linux: sudo systemctl restart docker

# 清理Docker资源
docker-compose down
docker system prune -a
docker-compose up -d
```

#### 问题2：端口被占用

**错误信息**:
```
Bind for 0.0.0.0:8080 failed: port is already allocated
```

**解决方法**:
```bash
# 查找占用端口的进程
# Windows
netstat -ano | findstr :8080

# Linux/macOS
lsof -i :8080

# 停止占用端口的进程，或修改docker-compose.yml中的端口映射
```

#### 问题3：数据库连接失败

**错误信息**:
```
Connection refused
```

**解决方法**:
```bash
# 检查数据库服务是否启动
docker-compose ps postgres

# 查看数据库日志
docker-compose logs postgres

# 重启数据库服务
docker-compose restart postgres

# 检查数据库连接
docker exec -it test-design-postgres psql -U postgres -d test_design_assistant
```

### 9.3 测试问题

#### 问题1：测试失败 - 数据库连接错误

**错误信息**:
```
org.postgresql.util.PSQLException: Connection refused
```

**解决方法**:
```bash
# 确保数据库服务已启动
docker-compose up -d postgres

# 检查测试配置文件
# backend-java/test-design-assistant-core/src/test/resources/application-test.yml
# 确保数据库连接配置正确
```

#### 问题2：测试失败 - Redis连接错误

**错误信息**:
```
Unable to connect to Redis
```

**解决方法**:
```bash
# 确保Redis服务已启动
docker-compose up -d redis

# 检查Redis连接
docker exec -it test-design-redis redis-cli ping
```

#### 问题3：测试超时

**错误信息**:
```
Test timed out after 10 seconds
```

**解决方法**:
```bash
# 增加测试超时时间
# 在测试类或测试方法上添加注解
@Timeout(value = 30, unit = TimeUnit.SECONDS)

# 或检查是否有长时间运行的操作
```

#### 问题4：测试覆盖率不达标

**错误信息**:
```
Coverage is below threshold
```

**解决方法**:
```bash
# 查看覆盖率报告，找出未覆盖的代码
# 补充测试用例

# 或临时降低覆盖率阈值（不推荐）
# 在pom.xml中修改jacoco配置
```

### 9.4 功能测试问题

#### 问题1：前端页面无法访问

**解决方法**:
```bash
# 检查前端服务是否启动
docker-compose ps frontend

# 查看前端日志
docker-compose logs frontend

# 检查端口是否被占用
# 访问 http://localhost:3000
```

#### 问题2：API接口返回404

**解决方法**:
```bash
# 检查后端服务是否启动
docker-compose ps backend-java

# 查看后端日志
docker-compose logs backend-java

# 检查API路径是否正确
# 访问 http://localhost:8080/api/swagger-ui.html 查看API文档
```

#### 问题3：用例生成失败

**解决方法**:
```bash
# 检查Python AI服务是否启动
docker-compose ps backend-python

# 查看Python服务日志
docker-compose logs backend-python

# 检查模型配置是否正确
# 访问 http://localhost:8080/api/v1/common/model-configs 查看模型配置

# 检查AI服务连接
curl http://localhost:8000/health
```

---

## 十、测试最佳实践

### 10.1 测试执行顺序

建议按以下顺序执行测试：

1. **单元测试**（最快，最基础）
   - Java后端单元测试
   - Python服务单元测试
   - 前端单元测试

2. **集成测试**（验证服务间集成）
   - 前后端集成测试
   - Java和Python服务集成测试
   - 数据库和Redis集成测试

3. **功能测试**（验证业务功能）
   - 需求管理功能测试
   - 用例管理功能测试
   - 用例生成功能测试
   - 其他功能测试

4. **性能测试**（验证性能）
   - 接口性能测试
   - 数据库性能测试
   - 用例生成性能测试

5. **安全测试**（验证安全性）
   - SQL注入防护测试
   - XSS防护测试
   - 权限验证测试

### 10.2 测试环境隔离

- **开发环境**: 用于日常开发，可以随时重启
- **测试环境**: 用于运行测试，保持稳定
- **预生产环境**: 用于最终验证，与生产环境一致

### 10.3 测试数据管理

- 使用独立的测试数据库
- 测试前清理测试数据
- 使用测试数据构建器（TestDataBuilder）
- 避免测试数据污染

### 10.4 持续集成

- 配置CI/CD自动运行测试
- 测试失败时阻止部署
- 定期查看测试报告
- 及时修复失败的测试

---

## 十一、测试报告模板

### 11.1 测试执行报告

**测试执行日期**: ___________  
**测试执行人**: ___________  
**测试环境**: 本地测试环境

#### 测试结果汇总

| 测试类型 | 测试用例数 | 通过数 | 失败数 | 跳过数 | 通过率 |
|---------|-----------|--------|--------|--------|--------|
| Java后端单元测试 | 231 | ___ | ___ | ___ | ___% |
| Python服务测试 | 25 | ___ | ___ | ___ | ___% |
| 前端测试 | 27 | ___ | ___ | ___ | ___% |
| 功能测试 | 30 | ___ | ___ | ___ | ___% |
| **总计** | **313** | **___** | **___** | **___** | **___%** |

#### 测试覆盖率

- Java后端覆盖率: ___%（目标 ≥ 70%）
- Python服务覆盖率: ___%（目标 ≥ 70%）
- 前端覆盖率: ___%（目标 ≥ 70%）

#### 发现的问题

1. ⬜ 无问题
2. ⬜ 问题1：________________（已修复/待修复）
3. ⬜ 问题2：________________（已修复/待修复）

#### 测试结论

- ⬜ ✅ 所有测试通过，可以继续下一步
- ⬜ ⚠️ 存在部分问题，需要修复后重新测试
- ⬜ ❌ 存在严重问题，需要修复

---

## 十二、快速参考

### 12.1 常用命令

```bash
# 启动所有服务
docker-compose up -d

# 停止所有服务
docker-compose down

# 查看服务状态
docker-compose ps

# 查看服务日志
docker-compose logs -f [服务名]

# 运行Java后端测试
cd backend-java/test-design-assistant-core
mvn clean test

# 运行Python服务测试
cd backend-python/ai-service
pytest

# 运行前端测试
cd frontend
npm run test:run

# 生成测试覆盖率报告
# Java: mvn clean test jacoco:report
# Python: pytest --cov=app --cov-report=html
# 前端: npm run test:coverage
```

### 12.2 服务访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端 | http://localhost:3000 | 前端应用 |
| Java后端 | http://localhost:8080 | REST API |
| Java后端API文档 | http://localhost:8080/api/swagger-ui.html | Swagger UI |
| Python AI服务 | http://localhost:8000 | AI服务API |
| Python AI服务API文档 | http://localhost:8000/docs | FastAPI文档 |
| Prometheus | http://localhost:9090 | 监控指标 |
| Grafana | http://localhost:3001 | 监控面板（admin/admin） |
| AlertManager | http://localhost:9093 | 告警管理 |
| Zipkin | http://localhost:9411 | 链路追踪 |

### 12.3 测试文件位置

- **Java后端测试**: `backend-java/test-design-assistant-core/src/test/java/`
- **Python服务测试**: `backend-python/ai-service/tests/`
- **前端测试**: `frontend/tests/`

### 12.4 测试报告位置

- **Java后端覆盖率报告**: `backend-java/test-design-assistant-core/target/site/jacoco/index.html`
- **Python服务覆盖率报告**: `backend-python/ai-service/htmlcov/index.html`
- **前端覆盖率报告**: `frontend/coverage/index.html`

---

## 附录

### A. 测试环境要求

- **操作系统**: Windows 10+ / Linux / macOS
- **Java**: 17+
- **Maven**: 3.6+
- **Python**: 3.10+
- **Node.js**: 18+
- **Docker**: 最新版
- **Docker Compose**: 最新版
- **内存**: 至少 8GB
- **磁盘空间**: 至少 10GB

### B. 相关文档

- [部署前测试验证清单](./部署前测试验证清单.md)
- [开发进度报告与路线图](./开发进度报告与路线图.md)
- [API接口文档](./API接口文档.md)
- [测试计划文档](./测试计划文档.md)

### C. 获取帮助

如果遇到问题，可以：

1. 查看本文档的"常见问题排查"章节
2. 查看服务日志：`docker-compose logs -f [服务名]`
3. 检查服务健康状态：访问健康检查接口
4. 查看相关文档

---

**文档版本**: 1.0  
**最后更新**: 2024-01-13  
**维护人**: 开发团队

---

*如有问题或建议，请及时反馈！*